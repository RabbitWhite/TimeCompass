You are the Architect. Plan only, no file modifications yet. Read the existing codebase thoroughly before proposing anything. When done, write your implementation plan to timecompass_ux_redesign_plan.md and wait for approval before any changes.
Context: TimeCompass is a PWA. We are doing a UX/UI redesign to reduce clutter and make the app more focused. The design has been fully specified. Your job is to plan the implementation precisely.
Navigation changes in BottomNav.tsx: Change tab order and labels to: Track, Status, Calendar, Stats, Areas. Track is tab 1 and the default route on app launch. Update App.tsx default route from slash to slash-track. Calendar tab points to slash-timeline. Remove Templates and Gamification as navigation destinations.
View 1 Track in Tracking.tsx: Keep start/stop quick-start buttons per area and manual entry modal and live timer. Remove weekly allocation editor, today's log list, link to templates, and this-week totals per area. Add a Catch Up section showing the 3 focus areas with the lowest percentage of period target achieved. Show area name and time behind target only. Do not display the percentage itself, it is used for ranking only. Use getPeriodIndex and getPeriodDateRange from utils.ts to determine current period boundaries.
View 2 Status in Dashboard.tsx: Keep active tracking live timer and euro reward card for current period only. Remove per-area weekly progress bars, upcoming calendar events list, and streak count. Add total used time vs period target as a single combined figure with a simple progress bar showing overall period completion.
View 3 Calendar in Timeline.tsx: No changes.
View 4 Stats in Statistics.tsx: Keep per-area breakdown, CSV export, period selector, actual vs target bar chart, daily activity bar chart, and pie chart. Remove summary stat cards showing total time, session count, avg per day, and avg per session. Change default sort order to lowest percentage of period target achieved first. Move gamification historical content here from Gamification.tsx: past periods summary table, weekly history bar chart, and score breakdown.
View 5 Areas in FocusAreas.tsx: Remove weekly progress bars and project count badges from area cards. Change FocusAreaDetail from a separate route to inline expansion within FocusAreas — tapping an area expands it to show and edit its projects and realizations inline. Weekly target hours must be editable directly on each expanded area card. Add a Manage Templates button that navigates to slash-templates. Keep all existing CRUD functionality for areas and projects.
Gamification.tsx: This file will be deleted after its content is redistributed as described above. Plan the redistribution carefully to avoid losing any functionality.
WeekTemplates.tsx: No changes to the file itself. Remove from navigation only. It remains accessible via the Manage Templates button in Areas view.
utils.ts: Add a new pure function called getCatchUpAreas that accepts focusAreas, timeEntries, settings, and n, and returns the n focus areas with the lowest percentage of period target achieved along with their time gap behind target. This function must be used by both Track view for the top 3 and Stats view for the full sorted list to ensure consistent ranking logic across both views.
App.tsx: Move gamification settings modal here into the existing header settings area. It is configuration, not a view.
Implementation order: Do these one file at a time in this sequence: utils.ts first, then BottomNav.tsx, then App.tsx, then Tracking.tsx, then Dashboard.tsx, then Statistics.tsx, then FocusAreas.tsx, then delete Gamification.tsx, then final check on WeekTemplates.tsx nav removal. After each file show the diff and wait for approval before proceeding to the next file.
Write the full implementation plan to timecompass_ux_redesign_plan.md. Include risks, any ambiguities you find in the existing code, and anything that needs a decision before implementation starts. Do not modify any source files yet.
